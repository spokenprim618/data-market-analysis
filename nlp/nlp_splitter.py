from schema import empty_schema

def nlp_split(text):
    sections = empty_schema()

    # placeholder logic (you will replace later)
    sections["unknown"].append(text)

    return sections, 0.5  # low confidence